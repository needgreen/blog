## 자바스크립트

### ⚙️ Node.js 설치 & 실행

https://nodejs.org

cmd 에서 node -v, npm -v 입력 버전확인

- vscode > code runner 확장 프로그램 설치

# 변수

- **숫자 타입 - Number**
  - 14 (정수/양수) 11.5(실수) -5(음수) 구분 없이 number 타입 처리
  - NaN (Not a Number) : 연산 불가
- **문자열 타입 - String**
  작은 따옴표 (’’), 큰 따옴표(””) 또는 백틱(``)으로 텍스트 표현
- 불리언 타입 - boolean
  true / false 논리 값
- null - 변수 값이 없음
- undefined - 선언 후 값을 할당하지 않은 변수
- symbol - 변경 불가능, 중복 되지 않은 값
- bigint - 2의 53승 - 1 보다 더 큰 숫자 표현

**변수 명명 규칙**

- 카멜케이스 (Camel Case)
  시작 문자는 소문자, 각 단어의 시작 문자를 대문자 작성
  `userName`
- 파스칼케이스 (Pascal Case)
  첫 단어의 시작 문자를 대문자로 작성
  `UserName`
- 스네이크케이스 (Snake Case)
  언더스코어(`_` )로 단어 연결
  `user_profile_name`
- 케밥케이스 (Kebab Case)
  하이픈(`-` )으로 단어 연결
  `user-profile-name`

**변수 선언**

- **var** (ES5 까지 사용하였으나 ES6 부터는 권장하지 않음)
- **let**
  - 재할당이 필요한 경우, 재할당 가능
  - 변수의 스코프는 최대한 좁게 만들어 사용 권장
  - 동일한 스코프(scope) 내 중복 선언 불가, 재선언(`let`) 불가능
  - 블럭 스코프 (선언된 블럭 내에서만 사용 가능)
  ```
  userName = '홍길동';
  userName = '고길동';
  // 재할당 가능
  ```
- **const**
  - 재할당 금지, 상수를 선언하기 위한 키워드
    - 상수 : 항상 동일한 수, 변경이 불가능한 값
  - 재선언 불가능, 재할당 불가능
  - 블럭 스코프 (선언된 블럭 내에서만 사용 가능)

# 📌 연산자 🔥

- 산술 연산자
  - 덧셈(+), 뺄셈(-) , 곱셈(\*), 나눗셈(/) , 나머지(%) , 제곱(\*\*)
  - 증감 연산자
    - 변수 값 1씩 증가/감소
    - `++` 증가, `--` 감소
    ```jsx
    +    // 더하기
    -    // 빼기
    *    // 곱하기
    /    // 나누기
    %    // 나머지
    **   // 거듭제곱
    ++   // 1 증가
    --   // 1 감소
    ```
- 대입 연산자
  - 오른쪽 값을 왼쪽 변수에 할당
    - **대입** (`=`)
    - **복합 대입** (`+=` `-=` `*=` `**=` `/=` `%=`)
    ```jsx
    =    // 할당
    +=   // 더한 후 할당 (a += 3은 a = a + 3과 같음)
    -=   // 뺀 후 할당
    *=   // 곱한 후 할당
    /=   // 나눈 후 할당
    %=   // 나머지를 구한 후 할당
    ```
- 비교 연산자
  - 두 피연산자 비교로 참/거짓 반환
    - **대소 비교** `>` `<` `>=` `<=`
      ```jsx
      >    // 크다
      <    // 작다
      >=   // 크거나 같다
      <=   // 작거나 같다
      ```
    - **동등 비교** : 타입이 맞지 않아도 타입의 값만 비교
      - `==` `!=`
      ```jsx
      ==   // 같다
      !=   // 값이 다름, 같지 않은지 비교
      ```
    - **일치 비교** : 타입 + 값 모두 비교
      - `===` `!==`
      ```jsx
      ===  // 타입과 값이 모두 같아야 함
      !==  // 타입 또는 값이 다름
      ```
- 논리 연산자
  - **AND** `&&`
    - 두 개의 논리값 모두 참일 때 최종 참 반환
    - 둘 중 하나라도 거짓일 경우 최종 거짓
  - **OR** `||`
    - 표시방법 shift + | 키
    - 두 개의 논리값 중 하나라도 참이면 최종 참 반환
    - 둘 다 거짓이어야 최종 거짓
  - **NOT** `!논리값`
    - 참일 경우 거짓 반환, 거짓일 경우 참 반환
    - 논리값 반대되는 조건의 경우
  ```jsx
  &&   // AND (그리고) - 둘 다 true일 때만 true
  ||   // OR (또는) - 하나라도 true면 true
  !    // NOT (부정) - true를 false로, false를 true로
  ```

# 📌 제어문

### 조건문

- **if 문**
  ```jsx
  if (조건식) { 조건이 참일 경우 실행할 구문; }
  } else { 조건이 거짓일 경우 실행할 구문; }
  } if { 조건식2) { 조건식2의 참일 경우 실행할 구문; }
  } else { 조건식1과 조건식2가 모두 거짓일 경우 실행할 구문 ;}
  ```
- **switch 문**
  ```jsx
  switch (표현식) {
  	case 값1 : 표현식 값1일 경우 실행할 구문;
  	break;
  	case 값2 : 표현식이 값2일 경우 실행할 구문;
  	break;
  	default : 표현식이 값1과 값2와 일치하지 않을 경우 실행할 구문;
  	break;
  }
  ```

### 반복문

- **`while` 문 : 조건이 참일 동안 반복 실행**
  ```jsx
  while(조건식) {
  	조건이 참일 경우 실행할 구문;
  }
  ```
- **do while 문**

  ```jsx
  do {
  	코드 블럭을 최소 한 번 실행;
  } while (조건식);
  ```

- **for 문 : 특정 횟수만큼 반복 실행**
  ```jsx
  for(초기식; 조건식; 증감식) {
  	조건이 참일 경우 실행할 구문;
  }
  ```

# 📌 함수(function) 🔥

### 함수 선언식

- 함수명으로 정의하는 방식
  ```jsx
  function 함수명 (변수) {
  	실행구문
  	리턴 결과;
  	}

  // 실제 작성 예시
  function hello(name) {
    console.log(`${name}님 환영해요.`);
  }
  hello('홍길동');
  ```
- 중복된 함수명의 경우 마지막 정의한 함수만 존재함.
- 함수 호출 가능 (호이스팅 특성을 갖고 있기 때문)
  `함수명(인자값);`

### 함수 표현식

- 함수를 변수에 저장하는 방식
- 함수명을 작성하지 않고 주로 익명함수를 변수에 저장하는 용도로 사용
  ```jsx
  const 변수명 = function (매개변수명) {
    실행구문;
    리턴결과;
  };

  // 호출법
  변수명();
  ```
  ```jsx
  const hi = function (name) {
    console.log(`${name}님 안녕하세요!`);
  };
  hi('김ㅋㅋ');
  ```
- 중복된 함수명 사용 불가 (재선언 불가)

### 매개 변수(parameter)

함수를 정의할 때 쓰는 변수 이름

- 변수 선언 시 키워드(var, let, const)는 사용하지 않음
- rest 변수 또는 arguments 배열을 이용해 0개 이상의 인자 처리 가능
  - argument(아규먼트)
    - 인자값 = 함수에 넘겨주는 값
    - 함수 내 암묵적으로 하나씩 존재하는 객체
  - rest parameter
    - 함수 정의에서 `...`을 붙여 **가변 인자(나머지 인자들)를 배열로 받는 문법**
    - 항상 마지막에 사용
    - `(...변수명)` 으로 표시

### 반환 (return)

- `return`은 함수가 **결과를 돌려주는 방법**
  `return`을 만나면 함수는 즉시 종료
  ```jsx
  // 두 수를 전달받아 덧셈연산 후
  // 결과를 반환하는 함수
  function sum(a, b) {
    console.log('덧셈연산 실행 중..');
    return a + b;
  }

  const result = sum(10, 20);
  console.log(result);
  ```

### 화살표 함수 (arrow function)

람다 함수라고 하며 함수를 더 짧게 쓰는 방법을 화살표 함수라고 함.

화살표 함수는 주로 배열 메서드, 간단한 연산, 콜백 함수에서 유용하게 사용함.

메서드 정의, 생성자 함수, 아규먼트 객체가 필요할 때는

- `function(){}` 키워드 대신 화살표(`=>`)를 사용하여 선언하는 방법.
- 익명함수만 화살표 함수로 정의할 수 있음.
- 화살표 함수는 arguments 지원안됨

```jsx
// 작성법
() => { 실행내용 }
 (): 매개변수 작성란
 {}: 함수내의 코드 작성란
```

- **함수표현식 기본 작성법**
  ```jsx
  // 예시 1. 매개변수 없음, 실행문 1개인 경우
  const fnExp1 = function () {
    console.log('fnExp1');
  };
  fnExp1();
  ```
- **화살표 함수 작성법**
  ```jsx
  // 예시 1. 화살표 함수 작성법

  const fnArrow1 = () => console.log('fnArrow1');
  // 단일 실행문은 {} 생략 가능
  fnArrow1();
  ```
  ```jsx
  // 예시 2. 매개변수 1개인 경우
  const fnArrow2 = (a) => {
    // 매개변수 '(a)' 1개인 경우 () 생략 가능
    console.log('fnExp2');
    console.log(a);
  };
  fnArrow2(10);
  ```
  ```jsx
  // 예시 3. 매개변수 여러개인 경우
  const fnArrow3 = (a, b) => {
    console.log('fnArrow3');
    console.log(a, b);
    return a + b;
  };
  console.log(fnArrow3(10, 20));
  ```

### 📌 콜백(callback) 함수 🔥🔥

인자로 다른 함수에 전달되어 나중에 실행되는 함수.

콜백함수를 매개변수로 받아 실행하는 함수는 고차함수라고 함.

- **콜백 함수 예시**

```jsx
function increase(value) {
  // 전달 값을 1 증가 시켜주는 함수
  return value + 1;
}
function decrease(value) {
  // 전달 값을 1 감소 시켜주는 함수
  return value - 1;
}
```

```jsx
// 변화 시킬 값을 전달받아서 상황에 따라
// 변화를 주도록 적용시켜주는 함수
function apply(value, func) {
  // value = 값, func(변수명) = 실행시킬 함수;
  return func(value);
}
```

```jsx
// 특정 값을 1 증가 시키고 싶음
console.log(apply(5, increase));
// increase : 콜백함수 apply: 고차함수
// console.log(increase(5)); 동일한 의도의 코드
```

```jsx
// 특정 값을 1 감소 시키고 싶음
console.log(apply(7, decrease));
// console.log(decrease(7)); 동일한 의도의 코드
```

```jsx
// 특정 값을 2배 증가 시키고 싶음
// 즉시 작성하여 실행하고자 할 때
console.log(
  apply(5, function (a) {
    return a * 2; // 코드 정석 버전
  })
);
console.log(apply(5, (a) => a * 2));
// 코드 축약 버전
```

# 객체 (object)

데이터와 함수(method)를 하나로 묶어 관리할 수 있는 자료 구조

- 속성(property, 프로퍼티) : 문자열
- 값(value) : 문자열, 숫자, boolean, 배열, 함수 등 모든 타입
- 객체 리터럴 정의 : { } 오브젝트, [ ] 배열 객체
- 메소드(method) : 객체 내 프로퍼티로 함수를 할당 시 메소드라고 함.

## 생성자 함수

객체를 만들어내는 템플릿, 같은 객체를 여러개 생성할 때 유용함.

- 함수명의 첫 글자는 대문자
- 화살표 함수로 작성 불가
- 프로퍼티를 함수 내 this.프로퍼티로 정의
- new 키워드와 함께 사용

```jsx
// 생성자 함수 (첫 글자를 대문자로!)
function Student(name, age, grade) {
  this.name = name;
  this.age = age;
  this.grade = grade;
}

// new 키워드로 객체 생성
let student1 = new Student('김철수', 20, 1);
let student2 = new Student('이영희', 21, 2);
let student3 = new Student('박민수', 22, 3);

console.log(student1); // {name: '김철수', age: 20, grade: 1}
console.log(student2); // {name: '이영희', age: 21, grade: 2}
```

- this 는 새로 만들어질 객체
  ```jsx
  function Car(brand, color) {
    // this = 새로 만들어질 객체
    this.brand = brand; // 새 객체에 brand 속성 추가
    this.color = color; // 새 객체에 color 속성 추가
    this.getInfo = function () {
      // 메소드 추가
      return `브랜드는 ${this.brand}이고 컬러는 ${this.color}입니다.`;
    };
  }

  const myCar = new Car('Tesla', 'red');
  console.log(myCar);
  // 출력 Car { brand: 'Tesla', color: 'red', getInfo: [Function (anonymous)] }
  console.log(myCar.getInfo());
  // 출력 브랜드는 Tesla이고 컬러는 red입니다.
  ```

## ES6 객체 관련 문법

### _⭐_ **속성(프로퍼티) 축약**

- 변수명과 키(key) 이름이 같을 때 사용
- 축약 문법 예시
  ```jsx
  const name = '김ㅋㅋ';
  const age = 32;

  const person = { name, age }; // 단축 구문
  console.log(person);
  //출력 { name: '김ㅋㅋ', age: 32 }
  ```

### _⭐_ **구조 분해 할당**

- 객체의 속성을 변수로 사용
- 변수명을 객체의 프로퍼티명과 동일해야 함.
  `{변수1, 변수2, ..} = 객체;`
  ```jsx
  const person = {
    name: '이영희',
    age: 30,
    city: 'Seoul',
  };

  // 기본 방식
  const name = person.name;
  const age = person.age;
  const city = person.city;

  // ES6 문법 구조 분해
  const { name, age, city } = person;

  console.log(name); // '이영희'
  console.log(age); // 30
  console.log(city); // 'Seoul'
  ```

### 메소드 단축

- 객체 내 함수를 단축하여 사용 (`function` 생략)
  ```jsx
  // 기본 방식
  const sum = {
    add: function (a, b) {
      return a + b;
    },
    subtract: function (a, b) {
      return a - b;
    },
  };

  // ES6 단축 구문
  const sum = {
    // function 키워드 생략!
    add(a, b) {
      return a + b;
    },
    subtract(a, b) {
      return a - b;
    },
  };

  console.log(sum.add(5, 3)); // 8
  console.log(sum.subtract(5, 3)); // 2
  ```

# 📌 프로토타입(prototype)
